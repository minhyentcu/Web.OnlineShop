@model IEnumerable<Web.OnlineShop.Entity.UserGroup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section header{
    Danh sách Nhóm người dùng
    }

    <p class=" row">
        <div>
            <a class="btn btn-xs btn-success float-md-right " href="@Url.Action("Create","UserGroup")">Tạo mới</a>
        </div>
    </p>
<table class="table table-striped table-bordered dt-responsive nowrap" id="dataTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
   

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@

            <a href="@Url.Action("Edit","UserGroup",new { id=item.Id })"><i class="fas fa-edit" style='font-size:24px'></i></a>
            <a href="@Url.Action("Details","UserGroup",new { id=item.Id })"><i class="fas fa-bars" style='font-size:24px'></i></a>
            <a href="@Url.Action("Delete","UserGroup",new { id=item.Id })"><i class="fas fa-trash-alt" style='font-size:24px'></i></a>
        </td>
    </tr>
}

</table>
<script>
    $('#dataTable').dataTable({
    });
</script>
